interface:
  display_name: "Flightdeck Repo Bridge"
  short_description: "Configure safe repository policy bridges"
  default_prompt: "Use $flightdeck-repo-bridge to configure declared bridges and verify saved projects by exact path with separate logical and runtime IDs."
