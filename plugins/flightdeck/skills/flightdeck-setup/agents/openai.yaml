interface:
  display_name: "Flightdeck Setup"
  short_description: "Generate a portable Flightdeck workspace"
  default_prompt: "Use $flightdeck-setup to generate and validate a new Flightdeck in an empty target, then verify its opaque runtime project ID by exact path."
