# Environments and runtime validation

Keep environment topology and validation notes here. Remote inspection and
shared-environment mutation are distinct operations; mutation and deployment
always require explicit approval.

Use `docs/workflows/platform.md` to separate infrastructure source, generated
plans, applied state, and observed runtime evidence. Use
`docs/workflows/ci-cd.md` when delivery pipelines produce or promote the
artifacts being validated.
