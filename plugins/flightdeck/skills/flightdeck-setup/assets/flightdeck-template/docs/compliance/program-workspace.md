# Program workspace

Create a program by copying `compliance/_program-template/` to a safe,
lowercase program identifier. Keep source documents and real evidence out of
tracked plugin content. Store local sensitive material only inside the program
workspace and follow its classification and retention rules.

The program `AGENTS.md` may strengthen Hub policy. Repository rules own code,
commands, and tests; the stricter security or authorization rule wins.

