interface:
  display_name: "Flightdeck"
  short_description: "Coordinate portable multi-project work"
  default_prompt: "Use $flightdeck to route this request by exact project path, keep logical and runtime project identities separate, and return without monitoring."
