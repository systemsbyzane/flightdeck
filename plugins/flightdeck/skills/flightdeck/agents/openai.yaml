interface:
  display_name: "Flightdeck"
  short_description: "Route direct work or opt-in missions safely"
  default_prompt: "Use $flightdeck to route this request by exact project path, preserve ordinary receipt-and-stop dispatch, and select $flightdeck-mission only for explicit mission intent with exact authorized targets and criterion-bound fan-in."
